import sys

common_friends = {}
current_user = 

for row in sys.stdin:
    names = row.split('->')
    user = names[0].strip()
    friends = names.split(' ')
    

from pprint import pprint
pprint(tr_stat)
